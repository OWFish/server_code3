#include "../interface/redis_api.h"
#include "../../redis/RedisConnect.h"
#include "StdAfx.h"

namespace LRedis
{
	/*
void LRedis::Zadd(char* rankname, int score, char* actordata)
	{
		static GameEngine* ge = GetGameEngine();
		auto pRedis = ge->GetRedis();
		if (pRedis && !pRedis->zadd(rankname, score, actordata))
		{
			OutputMsg(rmError, ("Redis Not Connected Successful , Error Info : %s"), pRedis->get_last_err());
		}
	}
	*/
	
}

